name: flutter_local_notifications_windows
description: Windows implementation of the flutter_local_notifications plugin (stub — FFI AOT workaround)
version: 3.0.0-stub
homepage: https://github.com/MaikuB/flutter_local_notifications/tree/master/flutter_local_notifications_windows

environment:
  sdk: ^3.10.0
  flutter: ">=3.38.1"

dependencies:
  flutter:
    sdk: flutter
  flutter_local_notifications_platform_interface: ^11.0.0
  meta: ^1.11.0
  timezone: ^0.11.0

flutter:
  plugin:
    implements: flutter_local_notifications
    platforms:
      windows:
        dartPluginClass: FlutterLocalNotificationsWindows
